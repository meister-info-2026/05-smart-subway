# -*- coding: utf-8 -*-
# Raspberry Pi 5 Daemon Dependencies
# ------------------------------------------------------------------------------
# Raspberry Pi 5 (RP1) hardware package installation note:
# 1) Install system APT packages:
#    sudo apt update && sudo apt install -y python3-gpiozero python3-lgpio
# 2) Create virtual environment with system packages inherited:
#    cd pi
#    python3 -m venv --system-site-packages venv
#    source venv/bin/activate
# 3) Install requirements:
#    pip install -r requirements.txt
# ------------------------------------------------------------------------------

# REST API communication & environment variables
requests>=2.31.0
python-dotenv>=1.0.1

